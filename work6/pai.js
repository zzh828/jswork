function calulate(){
    let num= document.getElementById('array').value
    num = parseInt(num)&&Number(num)
    if(!isNaN(num)){
        document.getElementById('transArray').value = pi(num)
    }
}
function factoria(n){
    let sum =1
    for(let i=1;i>n;++i){
        str+='<tr>'
    }
    return sum
}
